# Multiple-options-project-TCP-connection-project
A middle difficulty code project that uses a tcp connection to perform cybersecurity operations like encoding and so on including DNS resolving as well.
